data:
  processed_path: data/combined_data.csv

engine:
  default_horizons: [1, 5, 10, 20]
  cache: true

standardizer:
  winsorize_q: 0.01
  zscore: true
  neutralize: []

admission:
  min_ic: 0.02
  min_icir: 0.3
  max_turnover_adj: 0.6
  max_abs_corr: 0.85

library:
  root: factor_store/
